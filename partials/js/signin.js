/* global angular */
var app = angular.module('KnackApp', ["firebase"]);

app.controller('SignInController', ['$scope', function ($scope) {
  	//  var dbRef = new Firebase("https://knackio.firebaseio.com");

     alert('hello from sign in');

  }
]);